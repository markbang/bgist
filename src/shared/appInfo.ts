export const appVersion = (require('../../package.json') as {version: string}).version;
export const appRepositoryOwner = 'markbang';
export const appRepositoryName = 'bgist';
export const appRepositoryUrl = `https://github.com/${appRepositoryOwner}/${appRepositoryName}`;
